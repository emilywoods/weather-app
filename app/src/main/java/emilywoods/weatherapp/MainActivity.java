package emilywoods.weatherapp;
/*
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import java.io.IOException;

import okhttp3.OkHttpClient;
import retrofit2.Retrofit;
import retrofit2.converter.gson.GsonConverterFactory;

public class MainActivity extends AppCompatActivity {

    private static OkHttpClient.Builder client = new OkHttpClient.Builder();

    public static final String BASE_URL = "http://localhost:3000/api/v1/locations/14/update_weather";

    private static Retrofit.Builder builder = new Retrofit.Builder()
    Retrofit retrofit = new Retrofit.Builder()
        .baseUrl(BASE_URL)
        .addConverterFactory(GsonConverterFactory.create())
        .build();

    return retrofit;

    public static void main(String[] args) throws IOException {
        MainActivity mainActivity =new MainActivity();
        CurrentWeather currentWeather = mainActivity.run("http://localhost:3000/api/v1/locations/14/update_weather");
    }





    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
    }
}
*/